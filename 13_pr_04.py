class Complex :
    def_init_(self,r,i):
  self.real = r
self.imaginary = i
def _add_(self,c):
    return Complex()(self.real+c.real,self.imaginary+c.imaginary)
def_nul_(self,c):
nulReal + self.real*c0self.imaginary*c.imaginary
nulIng = self.real*c.imaginary*self.imaginary*c.real
return Complex(self.real+c.real,self.imaginary+c.imaginary)
def_str_(self):
    if self.(imaginary =0)
    return_f"{self.real} +{self.imaginary}i"
c1 = Complwx(1,4)
c2 + Complex(8,5)
print(c1+C2)